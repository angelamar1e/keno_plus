import 'package:keno_plus/core/values/app_imports.dart';

class Profile extends StatelessWidget {
  const Profile({super.key});

  @override
  Widget build(BuildContext context) {
    return const KenoSignUpWidget();
  }
}
